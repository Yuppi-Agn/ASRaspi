<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context=".ui.addlecture.AddLectureFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Горизонтальный контейнер для выбора предмета и даты -->
        <LinearLayout
            android:id="@+id/top_controls_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Выпадающий список (Spinner) для выбора предмета -->
            <Spinner
                android:id="@+id/spinner_subject"
                android:layout_width="0dp"
                android:layout_height="56dp"
                android:layout_weight="1"
                android:background="@drawable/spinner_background"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:layout_marginEnd="8dp"
                tools:prompt="Дискретная математика"/>

            <!-- Контейнер для выбора даты -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/date_picker_layout"
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:hint="Дата"
                app:boxStrokeColor="@color/purple_500">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/et_date"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:focusable="false"
                    android:clickable="true"
                    android:drawableEnd="@drawable/ic_calendar_month_24"
                    android:drawablePadding="8dp" />
            </com.google.android.material.textfield.TextInputLayout>
        </LinearLayout>

        <!-- Большое поле для ввода текста лекции -->
        <EditText
            android:id="@+id/et_lecture_notes"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            android:background="@android:color/transparent"
            android:gravity="top|start"
            android:hint="Начните писать конспект лекции..."
            android:inputType="textMultiLine|textCapSentences"
            android:padding="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/top_controls_container" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Плавающая кнопка (FAB) для сохранения -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_save_lecture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="24dp"
        android:src="@drawable/check_24"
        app:backgroundTint="#EADDFF"
        app:tint="#6750A4"
        app:elevation="4dp"
        android:contentDescription="Сохранить запись"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
